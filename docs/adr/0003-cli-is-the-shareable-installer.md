# The CLI is the shareable installer

The Host proves a Screen. Strangers still need a way to take the files into
their own project. An editor dock would fit Godot better later, but a CLI can
be shared now: `init` copies tokens, `add` copies a piece and its
dependencies, and existing files stay owned unless `--force` is passed. The
CLI is not a runtime dependency.
