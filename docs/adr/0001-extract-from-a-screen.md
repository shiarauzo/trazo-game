# Extract Trazo from a Screen, not an installer

The first slice could have been an editor dock that copies tokens and a Button
into an empty project. That would prove install, not whether focus, prompts,
and an overlay survive inside a game. We start from a playable Screen and
extract Trazo from what that Screen actually needs. The installer waits.
